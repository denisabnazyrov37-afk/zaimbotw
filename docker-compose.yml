services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: loanbot
      POSTGRES_USER: loanbot
      POSTGRES_PASSWORD: loanbot
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U loanbot -d loanbot"]
      interval: 5s
      timeout: 5s
      retries: 10

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./data:/app/data

volumes:
  postgres_data:
